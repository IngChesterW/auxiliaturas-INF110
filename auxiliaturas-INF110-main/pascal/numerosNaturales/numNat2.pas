//preguntar si a es divisible entre b 
program numNat2;
uses crt;
  var num1, num2 : integer;
begin 
readln(num1);
readln(num2);
if (num1  mod num2 ) =  0 then
    begin
     writeln('a es divisible a b');
    end

end.
